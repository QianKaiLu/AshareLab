# TODO

按优先级排。完成后移到 CHANGELOG，并在需求文档里补上对应设计。

## 高：仓位与择时层（本轮明确推后）

现状：数据模型里没有账户资金，算不出仓位百分比，所以下面这些已写进交易系统的规则目前**无法执行**：

- `活跃市值.md`：活跃市值 -2.3% 控制的下跌波段 → 再完美的票顶多 1~2 成仓；4% 以上新波段 → 可满仓
- `对称交易.md`：大盘处于守恒（横盘）状态 → 只卖不买、不选股
- modoo（5）长线短线仓位配置比例、拒绝梭哈

落地时需要：

1. `account.json` 手工维护账户总资金
2. 日报加「市场层」段：大盘状态 / 0AMV 读数 → 建议仓位上限 → 当前实际总仓位与每票占比
3. `manual_data/0AMV.csv` 目前是空的，0AMV 没录入时这段跳过并标注

## 中：卖点精细化

本轮只要「大体符合交易理念」，不做「条件 → 动作 → 仓位比例」的精确映射。后续若发现日报的卖点建议前后不一致，再补：

- 「BBI 上两根中大阳线」里「中大阳」的量化口径（可用 `indicators/price_limit.py` 的 `body_norm` 按板块幅度归一化实体）
- 信号冲突优先级表（BBI 减仓 vs J 值钝化放飞；出货 S1 vs 加速段拉升）

## 中：长期价值投资分支

`qk-stock-value-*` 系列，与波段完全独立（需求文档 D7）。周线 B1、分批建仓、不止损但要等合理价格 —— 规则形态不同，不要复用波段的 skill。

## 低：便签到期提醒的触发方式

需求文档 D5 定了便签带 `status` + `due`，但没定谁来触发回看。两个方向：对话中 AI 主动检查（简单，依赖每天跑日报）vs 定时脚本推送。先用前者，不够用再说。

## 低：N 型结构上一个低点的自动识别

摆动点检测阈值敏感，客观识别困难。本轮不做自动识别 —— 止损计划由人工观察确定，AI 只提供建议并说明用的是哪一天的低点（需求文档 D9）。
