<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        /* === 重置与基础布局 === */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
            min-height: 100vh;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* === 卡片容器：居中内容，限制宽度 === */
        .card-container {
            width: 600px;
            background: {{ background_color }}CC;
            position: relative;
            overflow: hidden;
            padding: 30px;
            border-radius: 12px;
            overflow: hidden;
        }

        /* === 头部区域：标题 + 描述 === */
        .header {
            margin-bottom: {{ '20px' if desc else '25px' }};
            text-align: center;
        }

        .title {
            font-size: 30px;
            font-weight: 600;
            color: {{ text_color }};
            margin-bottom: 12px;
            line-height: 1.3;
        }

        .desc {
            font-size: 16px;
            line-height: 1.6;
            color: {{ text_color }}CC;
            max-width: 90%;
            margin: 0 auto;
            white-space: pre-line;
            text-align: center;
        }

        /* === 图片区域：垂直堆叠，带内边距控制 === */
        .images-section {
            margin: 10px 0;
        }

        .image-container {
            margin-bottom: 20px;
            overflow: hidden;
        }

        .image-container:last-child {
            margin-bottom: 0;
        }

        .kline-image {
            width: 100%;
            height: auto;
            display: block;
            max-height: 1000px;
            object-fit: contain;
        }

        /* === 页脚区域：带分割线的底部说明 === */
        .footer {
            margin-top: 20px;
            padding-top: 10px;
            border-top: 1px solid {{ text_color }}19;
        }

        .note {
            font-size: 14px;
            line-height: 1.8;
            color: {{ text_color }}99;
            white-space: pre-line;
            width: 100%; /* 与图片容器同宽，保证左对齐时视觉一致 */
            text-align: left; /* 左对齐（明确声明，避免继承干扰） */
        }
    </style>
</head>
<body>
    <div class="card-container">
        <!-- 头部：条件渲染描述文案 -->
        <div class="header">
            <h1 class="title">{{ title }}</h1>
            {% if desc %}
            <div class="desc">{{ desc }}</div>
            {% endif %}
        </div>

        <!-- 图片区域：循环渲染 base64 图片，每张独立容器 -->
        <div class="images-section">
            {% for img_base64 in images %}
            <div class="image-container">
                <img 
                    src="data:image/png;base64,{{ img_base64 }}" 
                    class="kline-image" 
                    alt="K线图 {{ loop.index }}"
                >
            </div>
            {% endfor %}
        </div>

        <!-- 页脚：条件渲染底部备注 note -->
        <div class="footer">
            {% if note %}
            <div class="note">{{ note }}</div>
            {% endif %}
        </div>
    </div>
</body>
</html>